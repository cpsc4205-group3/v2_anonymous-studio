# Anonymous Studio — Python dependencies
#
# ⚠️  PYTHON VERSION: 3.10, 3.11, 3.12, or 3.13 ONLY
#
# Python 3.14 is NOT supported. Presidio and spaCy do not yet publish
# wheels for 3.14. Create your venv with an older version explicitly:
#
#   python3.12 -m venv .venv        (recommended)
#   python3.11 -m venv .venv
#
# If you only have 3.14 installed, get 3.12 from https://python.org first.
# Check your version: python --version  (must show 3.12.x or similar)

taipy>=3.1.0,<4.2
presidio-analyzer>=2.2.35
presidio-anonymizer>=2.2.35
spacy>=3.7.0,<3.9
pandas>=2.0.0
openpyxl>=3.1.0
faker>=24.0.0